# 服务监控与告警

## 服务监控

进入 **服务** 页面（`/monitor`）可以创建对目标地址的可用性监控。

### 监控类型

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| HTTP GET | 发送 GET 请求，根据状态码判断可用性 | 网站/API |
| ICMP Ping | ICMP 探测 | 服务器连通性 |
| TCP Ping | TCP 端口连通性测试 | 端口服务 |

### 创建监控

1. 点击 **添加监控**。
2. 填写：
   - **名称**：监控项名称
   - **类型**：HTTP / ICMP / TCP
   - **目标**：URL、IP 或 `IP:端口`
   - **检测间隔（秒）**：两次检测之间的时间
   - **通知组**：触发告警时使用的通知方式
   - **覆盖范围**：全部服务器 / 仅特定服务器 / 忽略特定服务器
   - **延迟下限/上限**：可选，延迟超出范围时触发告警
   - **失败触发任务 / 恢复触发任务**：可选，联动计划任务
   - **在服务页展示**：是否在前台服务页面显示
3. 保存后监控任务会立即开始执行。

### 监控结果

- 在 **服务** 页面查看最近 24 小时可用率、平均延迟
- 点击监控项可查看历史曲线
- 通过 API 获取历史：`GET /api/v1/monitor/:id`

---

## 告警规则

告警规则用于根据服务器自身指标触发通知。

### 进入方式

在 **通知方式** 页面（`/notification`）的下方可以管理告警规则。

### 规则类型

| 类型 | 说明 |
|------|------|
| `cpu` | CPU 占用率 |
| `memory` | 内存占用率 |
| `swap` | Swap 占用率 |
| `disk` | 磁盘占用率 |
| `net_in_speed` / `net_out_speed` / `net_all_speed` | 入站/出站/总网络速率 |
| `transfer_in` / `transfer_out` / `transfer_all` | 入站/出站/总流量 |
| `transfer_in_cycle` / `transfer_out_cycle` / `transfer_all_cycle` | 周期流量统计 |
| `offline` | 服务器离线 |
| `load1` / `load5` / `load15` | 系统负载 |
| `tcp_conn_count` / `udp_conn_count` | TCP/UDP 连接数 |
| `process_count` | 进程数 |
| `temperature_max` | 最高温度 |

### 规则参数

- **名称**：规则名称
- **类型**：上述指标类型
- **最小值 / 最大值**：触发阈值
- **持续周期**：连续多少次检测满足条件后触发
- **通知组**：使用哪种通知方式
- **覆盖范围**：对哪些服务器生效
- **触发模式**：
  - `0` 始终触发：每次满足条件都发送通知
  - `1` 单次触发：同一轮异常只发送一次

### 告警恢复

当指标回到正常范围后，系统会自动发送恢复通知（取决于通知方式配置）。
